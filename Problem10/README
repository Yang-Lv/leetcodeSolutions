时间：2016/5/21
题目：leetcode Problem11小节
本题大意就是说给我两个字符串，一个作为模式串，一个是源串，判断源串是否可以被模式串匹配。其中需要处理两个特殊的字符,'*'和'.'，和正则表达式里一致，'*'代表前面的字符出现的次数>=0,'.'代表通配符
解法：
目前有两种比较经典的方法，回溯法和动态规划。
回溯法：
我们观察模式串，如果当前字符不是特殊字符，我们只需要比较是否和源串相应位置的字符相同即可。如果当前字符是‘*’那就比较麻烦了，因为我们根本不知道'*'前的那个字符到底出现几次，那么回溯法的做法就是枚举所有的可能，对于每一种可能，往前继续匹配，失败了就回溯到枚举的地方，选择下一个候选可能，继续匹配。。。
动态规划：
与回溯法思想类似，但是它的优势在于速度更加快。以一个二位数组来模拟整个匹配的过错。dp[i][j]代表src[0-i-1],pattern[0,j-1]是否匹配。
具体算法过程，可以看C++源码。
